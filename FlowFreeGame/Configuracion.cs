namespace FlowFreeSolver
{
    /// Clase estática para almacenar configuraciones globales del solver.
    public static class Configuracion
    {
        /// Nivel de depuración:
        /// 0 = Sin depuración,
        /// 1 = Básico,
        /// 2 = Detallado,
        /// 3 = Muy detallado. <summary>
        public static int NivelDepuracion = 3;
    }
}
